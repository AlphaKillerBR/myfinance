/*
 * config.js — configuração local do app.
 *
 * Substitua o valor abaixo pelo Client ID OAuth que você criar no
 * Google Cloud Console. Instruções completas no README.md.
 *
 * Enquanto estiver com o valor padrão, o app funciona normalmente,
 * só o botão "Conectar Google Drive" fica desativado.
 */
const CONFIG = {
  GOOGLE_CLIENT_ID: 'SEU_CLIENT_ID_AQUI',
};
